name: Build and Test Wordle

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "latest"
          cache: "npm"

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check TypeScript
        run: pnpm tsc --noEmit

      - name: Build project
        run: pnpm build

      - name: Test Next.js build output
        run: |
          if [ ! -d ".next" ]; then
            echo "❌ Next.js build output directory '.next' not found"
            exit 1
          fi
          echo "✅ Next.js build output verified successfully"

      - name: Build for Cloudflare
        run: pnpm build:cloudflare

      - name: Test Cloudflare build output
        run: |
          if [ ! -d ".open-next" ]; then
            echo "❌ Cloudflare build output directory '.open-next' not found"
            exit 1
          fi
          echo "✅ Cloudflare build output verified successfully"
